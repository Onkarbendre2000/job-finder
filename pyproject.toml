[project]
name = "job-finder-agent"
version = "0.1.0"
description = "Public ATS job finder and scoring dashboard"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
